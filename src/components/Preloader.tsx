// Preloader is now handled via inline HTML in index.html for instant first paint.
// This component is kept as a no-op for backward compatibility.
export default function Preloader() {
  return null;
}
